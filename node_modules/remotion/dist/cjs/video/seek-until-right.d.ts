import { type LogLevel } from '../log';
export declare const seekToTime: ({ element, desiredTime, logLevel, }: {
    element: HTMLVideoElement;
    desiredTime: number;
    logLevel: LogLevel;
}) => {
    wait: Promise<number>;
    cancel: () => void;
};
export declare const seekToTimeMultipleUntilRight: ({ element, desiredTime, fps, logLevel, }: {
    element: HTMLVideoElement;
    desiredTime: number;
    fps: number;
    logLevel: LogLevel;
}) => {
    prom: Promise<void>;
    cancel: () => void;
};
