import { ThebeEvents } from './events';
import type { BinderOptions, KernelOptions, CoreOptions, SavedSessionOptions, ServerSettings, MathjaxOptions } from './types';
export declare class Config {
    private _options;
    private _binderOptions;
    private _savedSessions;
    private _kernelOptions;
    private _serverSettings;
    private _events;
    constructor(opts?: CoreOptions, extraConfig?: {
        events?: ThebeEvents;
    });
    get events(): ThebeEvents;
    get base(): Required<Omit<CoreOptions, "binderOptions" | "savedSessionOptions" | "kernelOptions" | "serverSettings">>;
    get mathjax(): MathjaxOptions;
    get binder(): Required<BinderOptions>;
    get savedSessions(): Required<SavedSessionOptions>;
    get kernels(): Required<KernelOptions>;
    get serverSettings(): Required<Omit<ServerSettings, 'wsUrl'>> & {
        wsUrl?: string;
    };
    set serverSettings(newSettings: Required<Omit<ServerSettings, 'wsUrl'>> & {
        wsUrl?: string;
    });
}
