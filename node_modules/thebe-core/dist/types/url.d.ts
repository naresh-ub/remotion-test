import type { Config } from './config';
import type { BinderUrlSet, RepoProviderSpec } from './types';
export declare function makeDefaultStorageKey(storagePrefix: string, url: string): string;
export declare const GITHUB_SPEC: RepoProviderSpec;
export declare const GITLAB_SPEC: RepoProviderSpec;
export declare const GIT_SPEC: RepoProviderSpec;
export declare const GIST_SPEC: RepoProviderSpec;
export declare const WELL_KNOWN_REPO_PROVIDERS: RepoProviderSpec[];
/**
 * Make a binder url for both well known or custom providers
 *
 * Custom providers are supported by passing in an array of CustomRepoProviderSpecs.
 *
 */
export declare function makeBinderUrls(config: Config, repoProviders: RepoProviderSpec[]): BinderUrlSet;
